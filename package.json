{
  "name": "its-humss-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "npm start --prefix server",
    "install-server": "npm install --prefix server",
    "dev": "npm run dev --prefix server"
  }
}
